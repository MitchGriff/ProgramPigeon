version: "3.9"

# Production overrides — use alongside docker-compose.yml:
#   docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d

services:

  backend:
    # Use the optimised multi-stage build; no bind mount or hot reload
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
    volumes: []  # No bind mount in production
    restart: unless-stopped

  frontend:
    # Override with the nginx-based production build
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: node:20-alpine  # Overridden by build above
    command: []            # nginx handles serving
    ports:
      - "80:80"
    restart: unless-stopped

  db:
    restart: unless-stopped
